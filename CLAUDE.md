# CLAUDE.md

Read [`AGENTS.md`](AGENTS.md) — it is the canonical guidance for any coding agent working in this
repo (architecture, build/validate commands, hard constraints). This file only exists as a thin
pointer for tooling that specifically looks for `CLAUDE.md`; don't duplicate `AGENTS.md`'s content
here, and if the two ever drift, update `AGENTS.md` and keep this file as-is.

For current project status, what's in flight, and what to do next, read **`RESUME_HERE.md`** in
full before touching anything — it is this repo's single source of truth for session-to-session
handoff, updated by every agent/reviewer as work lands.

If you're here as part of the self-improvement upgrade system (`docs/upgrades/ARCHITECTURE.md`),
you were also given a specific task's scope — `AGENTS.md`'s constraints section covers what "in
scope" means for that context specifically.
