{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "presence",
  "description": "Minimal capability for the presence overlay window — only needs event IPC to receive state from the main window.",
  "windows": ["presence"],
  "platforms": ["windows", "linux"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
