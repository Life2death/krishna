{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "teleprompter",
  "description": "Minimal capability for the teleprompter overlay window - event listening + the bare window controls the in-window UI needs (close button, drag header, resize handles).",
  "windows": ["teleprompter"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging"
  ]
}
